import { createTheme } from "@mui/material"

export const theme = createTheme({
	palette: {
		primary: { main: "#0D1A2D" },
		secondary: { main: "#ff7f50" },
		success: { main: "#DFFFD8" },
        info:{main:"#B4E4FF"}
	}
})
